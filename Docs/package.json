{
  "name": "libtsmreactive-docs",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:generate": "wowlua_ls doc .. --out-dir api --class Reactive --class UIBindings --class UIManager --class ReactiveState --class ReactiveStream --class ReactiveStateSchema --class ReactivePublisher --class ReactivePublisherSchemaBase --class ReactivePublisherSchema --class ReactivePublisherSchemaShared",
    "docs:dev": "npm run docs:generate && vitepress dev .",
    "docs:build": "npm run docs:generate && vitepress build ."
  },
  "devDependencies": {
    "vitepress": "^1.0.0"
  }
}
